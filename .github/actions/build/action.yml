name: Build projects
description: Run dotnet build for a certain configuration
inputs:
  configuration:
    description: One of Debug or Release
    required: true
    type: string
    default: Release

runs:
  using: "composite"
  steps:
    - name: Build project
      shell: sh
      run: dotnet build --no-restore --configuration ${{ inputs.configuration }}
